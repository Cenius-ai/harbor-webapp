# Copy this file to `.env` and fill in the real values.
# `.env` is gitignored and should NEVER be committed.
# Values here are placeholders / examples ONLY.

# ─── Required ───

# dev-only; regenerate for production. App configuration value.
NITRO_SSL_KEY=cenius-dev-d5abad1e99c92fe48f9304dfc450be90

# ─── Optional ───

# App configuration value.
FETCH_KEEP_ALIVE=<set in .env>

# Provider: Runtime config
# Docs: Standard runtime configuration; not secret
# Runtime config configuration. Obtain from the provider dashboard.
HOST=<set in .env>

# App configuration value.
NITRO_ENV_EXPANSION=<set in .env>

# App configuration value.
NITRO_ENV_PREFIX=<set in .env>

# App configuration value.
NITRO_HOST=http://localhost:8080

# App configuration value.
NITRO_PORT=8080

# App configuration value.
NITRO_SHUTDOWN_DISABLED=<set in .env>

# App configuration value.
NITRO_SHUTDOWN_NO_FORCE_EXIT=<set in .env>

# App configuration value.
NITRO_SHUTDOWN_SIGNALS=<set in .env>

# App configuration value.
NITRO_SHUTDOWN_TIMEOUT=<set in .env>

# App configuration value.
NITRO_SSL_CERT=<set in .env>

# App configuration value.
NITRO_UNIX_SOCKET=<set in .env>

# Provider: Runtime config
# Docs: Standard runtime configuration; not secret
# Runtime config configuration. Obtain from the provider dashboard.
PORT=8080
